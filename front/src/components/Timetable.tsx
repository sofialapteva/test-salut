import React from 'react'

export interface TimetableProps {
  children: React.ReactNode
}

export function Timetable({children}: TimetableProps) {
  return (
    <div>
    Timetable
    </div>
  )
}

